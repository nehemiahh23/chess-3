active = false;

button_start();

//settings
set_obj[0] = obj_set_board; //settings objects

dest_x[0] = x; //settings positions

dist = 48; //settings distance between each

set_inst = ds_list_create(); //settings instances

